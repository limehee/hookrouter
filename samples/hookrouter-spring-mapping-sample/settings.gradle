rootProject.name = 'hookrouter-spring-mapping-sample'

includeBuild('../..') {
    dependencySubstitution {
        substitute(module('io.github.limehee:hookrouter-core')).using(project(':hookrouter-core'))
        substitute(module('io.github.limehee:hookrouter-spring')).using(project(':hookrouter-spring'))
    }
}
